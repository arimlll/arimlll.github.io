---
import BaseLayout from './BaseLayout.astro';
import TagList from '@/components/TagList.astro';
import Giscus from '@/components/Giscus.astro';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags?: string[];
  lang?: string;
  source?: string;
  wordCount?: number;
}

const { title, description, pubDate, updatedDate, tags = [], lang = 'zh', source, wordCount } = Astro.props;

const formattedPubDate = pubDate.toLocaleDateString('zh-CN', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});

const formattedUpdatedDate = updatedDate?.toLocaleDateString('zh-CN', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---
<BaseLayout title={title} description={description}>
  <article class="post" lang={lang}>
    <header class="post-header">
      <h1 class="post-title">{title}</h1>
      <div class="post-meta">
        <time datetime={pubDate.toISOString()}>{formattedPubDate}</time>
        {updatedDate && (
          <span class="updated">
            (更新于 <time datetime={updatedDate.toISOString()}>{formattedUpdatedDate}</time>)
          </span>
        )}
        {wordCount && (
          <span class="word-count">· {wordCount} 字 · 转载请标注来源</span>
        )}
        
      </div>
      <TagList tags={tags} />
    </header>
    <hr class="post-divider" />
    <div class="prose">
      <slot />
    </div>
    <Giscus lang={lang === 'ja' ? 'ja' : lang === 'en' ? 'en' : 'zh-CN'} />
  </article>
</BaseLayout>

<style>
  .post {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .post-header {
    margin-bottom: 1.5rem;
  }

  .post-title {
    font-size: 2rem;
    margin-bottom: 0.75rem;
    line-height: 1.3;
  }

  .post-meta {
    color: var(--color-text-secondary);
    font-size: 0.85rem;
    margin-bottom: 0.75rem;
  }

  .updated {
    margin-left: 0.25rem;
  }

  .word-count {
    margin-left: 0.25rem;
  }

  .post-source {
    font-size: 0.85rem;
    color: var(--color-text-secondary);
    margin-bottom: 0.75rem;
    padding: 0.5rem 0.75rem;
    background-color: var(--color-tag-bg);
    border-radius: 6px;
    border-left: 3px solid var(--color-accent);
  }

  .post-source a {
    color: var(--color-accent);
    word-break: break-all;
  }

  .post-notice {
    font-size: 0.8rem;
    color: var(--color-text-secondary);
    margin-bottom: 0.75rem;
  }

  .post-divider {
    border: none;
    border-top: 1px solid var(--color-border);
    margin: 1.5rem 0 2rem;
  }
</style>
